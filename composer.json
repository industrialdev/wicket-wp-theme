{
  "name": "industrialdev/wicket-wp-theme",
  "description": "Wicket Theme",
  "version": "2.1.21",
  "keywords": [
    "wordpress",
    "wicket",
    "theme"
  ],
  "homepage": "https://github.com/industrialdev/wicket-wp-theme",
  "authors": [
    {
      "name": "Wicket",
      "homepage": "https://wicket.io"
    }
  ],
  "type": "wordpress-theme",
  "require": {
    "php": ">=8.0"
  },
  "require-dev": {
    "brain/monkey": "^2",
    "friendsofphp/php-cs-fixer": "^3",
    "pestphp/pest": "^4",
    "phpunit/phpunit": "^12"
  },
  "autoload": {
    "psr-4": {
      "WicketTheme\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "WicketTheme\\Tests\\": "tests/unit/"
    }
  },
  "scripts": {
    "production": "rm -rf vendor && composer install --no-dev --optimize-autoloader",
    "setup-hooks": "mkdir -p .git/hooks && cp .ci/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push && echo 'Git hooks installed to .git/hooks/pre-push'",
    "post-install-cmd": [
      "@cs:fix"
    ],
    "post-update-cmd": [
      "@cs:fix"
    ],
    "test": "pest",
    "test:unit": "pest --testsuite unit",
    "test:coverage": "pest --coverage-html coverage",
    "test:browser": "WICKET_BROWSER_BASE_URL=https://localhost ./vendor/bin/pest --testsuite browser",
    "lint": "php-cs-fixer fix --dry-run --diff",
    "format": "php-cs-fixer fix",
    "check": [
      "@lint",
      "@test"
    ],
    "cs:fix": "php-cs-fixer fix"
  },
  "config": {
    "allow-plugins": {
      "pestphp/pest-plugin": true
    }
  }
}
